/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.dummyjson.com', 'www.shutterstock.com', 'img.freepik.com'], // Add the domain here
  },
};

export default nextConfig;
